# MyChain 项目概览

## 📋 项目信息

- **项目名称**：MyChain
- **框架版本**：Cosmos SDK v0.53.4
- **构建工具**：Ignite CLI
- **开发语言**：Go + HTML/CSS/JavaScript
- **完成时间**：2025年11月21日

## ✨ 已实现功能

### 1. ✅ 代币生产（Token Minting）

**实现位置**：`x/token/keeper/msg_server_mint_tokens.go`

**功能描述**：
- 支持铸造新代币到指定地址
- 从模块账户铸造代币并发送给接收者
- 支持自定义铸造数量和目标地址

**使用方式**：
```bash
mychaind tx token mint-tokens 10000000000token [接收地址] \
  --from [账户名] \
  --chain-id mychain \
  --yes
```

**技术实现**：
- 使用 BankKeeper.MintCoins 铸造代币到模块账户
- 使用 SendCoinsFromModuleToAccount 发送给接收者
- 完整的地址验证和错误处理

---

### 2. ✅ 新建用户（User Creation）

**实现位置**：Cosmos SDK 标准账户系统 + `explorer/index.html`

**功能描述**：
- 支持命令行创建账户
- 支持助记词恢复账户
- 浏览器界面生成助记词
- 完整的密钥管理系统

**使用方式**：
```bash
# 命令行创建
mychaind keys add [账户名]

# 恢复账户
mychaind keys add [账户名] --recover

# 浏览器创建：点击"创建账户"按钮
```

**技术实现**：
- 使用 Cosmos SDK 标准 keys 模块
- BIP39 助记词生成
- 本地密钥存储和加密

---

### 3. ✅ 代币转账（Token Transfer）

**实现位置**：`x/token/keeper/msg_server_transfer_tokens.go`

**功能描述**：
- 账户间转移代币
- 自动验证余额充足性
- 支持多种代币面额

**使用方式**：
```bash
mychaind tx token transfer-tokens [接收地址] 1000000000token \
  --from [发送者] \
  --chain-id mychain \
  --yes
```

**技术实现**：
- 使用 BankKeeper.SendCoinsFromAccountToAccount
- 地址格式验证
- 金额解析和验证
- 交易原子性保证

---

### 4. ✅ 矿工奖励（Miner Rewards）

**实现位置**：`x/token/keeper/msg_server_reward_miner.go`

**功能描述**：
- 区块奖励机制
- 固定每次奖励 10 tokens
- 可扩展为动态奖励

**使用方式**：
```bash
mychaind tx token reward-miner [矿工地址] \
  --from [账户名] \
  --chain-id mychain \
  --yes
```

**技术实现**：
- 铸造固定数量代币（10 tokens = 10,000,000 基本单位）
- 从模块账户发送给矿工
- 模拟 PoW 挖矿奖励

**奖励详情**：
- 每次奖励：10 tokens
- 铸造源：token 模块账户
- 可以扩展为基于区块高度的动态奖励

---

### 5. ✅ 区块链浏览器（Blockchain Explorer）

**实现位置**：`explorer/index.html`

**功能描述**：

#### 实时统计面板
- ⚡ 当前区块高度（实时更新）
- 🟢 节点状态监控
- 🔗 链 ID 显示

#### 用户功能
- 👤 生成新账户（含助记词）
- 💰 查询账户余额
- 📋 操作命令提示

#### 代币操作界面
- 🪙 铸造代币表单
- 💸 转账代币表单
- ⛏️  矿工奖励表单
- 📝 自动生成命令行指令

#### 区块浏览功能
- 📦 显示最新 10 个区块
- 🔍 区块详细信息：
  - 区块高度
  - 区块哈希
  - 创建时间
  - 提议者地址
  - 交易数量
- 🔄 自动刷新（每 10 秒）

**技术实现**：
- 纯 HTML/CSS/JavaScript
- RESTful API 集成（端口 1317）
- RPC API 集成（端口 26657）
- 响应式设计
- 实时数据轮询

**访问方式**：
```bash
# 方法 1：直接打开
file:///Users/dany/web3/homework/mychain/explorer/index.html

# 方法 2：HTTP 服务器
cd explorer && python3 -m http.server 8080
# 访问 http://localhost:8080
```

---

## 🏗️ 项目架构

### 模块结构

```
mychain/
├── app/                          # 应用层
│   ├── app.go                   # 应用初始化
│   └── app_config.go            # 模块配置
│
├── x/token/                      # Token 自定义模块
│   ├── keeper/                  # 业务逻辑层
│   │   ├── keeper.go           # Keeper 实现
│   │   ├── msg_server_mint_tokens.go      # 铸币逻辑
│   │   ├── msg_server_transfer_tokens.go  # 转账逻辑
│   │   ├── msg_server_reward_miner.go     # 奖励逻辑
│   │   └── query_balance.go               # 余额查询
│   │
│   ├── types/                   # 类型定义
│   │   ├── expected_keepers.go # Keeper 接口
│   │   ├── codec.go            # 编解码
│   │   ├── keys.go             # 常量定义
│   │   └── errors.go           # 错误定义
│   │
│   └── module/                  # 模块配置
│       ├── module.go           # 模块接口实现
│       ├── autocli.go          # CLI 自动生成
│       └── depinject.go        # 依赖注入
│
├── proto/mychain/token/v1/      # Protocol Buffers
│   ├── tx.proto                # 交易消息定义
│   ├── query.proto             # 查询定义
│   ├── genesis.proto           # 创世状态
│   └── params.proto            # 参数定义
│
├── explorer/                     # 区块链浏览器
│   └── index.html              # Web 前端
│
├── cmd/mychaind/                # 命令行工具
│
├── config.yml                   # 链配置
├── start.sh                     # 快速启动脚本
├── test_blockchain.sh           # 自动化测试脚本
├── README.md                    # 项目文档
└── USAGE_GUIDE.md              # 使用指南
```

### 技术栈

**后端（区块链）**：
- Cosmos SDK v0.53.4
- Tendermint Core（共识引擎）
- Go 1.21+
- Protocol Buffers
- gRPC

**前端（浏览器）**：
- HTML5
- CSS3（响应式设计）
- JavaScript（原生，无框架）
- RESTful API
- JSON-RPC

**工具**：
- Ignite CLI v28+
- Git
- Make

---

## 🔌 API 端点

### REST API（端口 1317）

#### Bank 模块
```
GET /cosmos/bank/v1beta1/balances/{address}
GET /cosmos/bank/v1beta1/supply
GET /cosmos/bank/v1beta1/supply/{denom}
```

#### Token 模块
```
GET /mychain/token/v1/params
GET /mychain/token/v1/balance/{address}
```

#### 节点信息
```
GET /cosmos/base/tendermint/v1beta1/node_info
GET /cosmos/base/tendermint/v1beta1/syncing
GET /cosmos/base/tendermint/v1beta1/blocks/latest
```

### RPC API（端口 26657）

```
GET /status                    # 节点状态
GET /block?height={h}          # 查询区块
GET /blockchain?minHeight={min}&maxHeight={max}
GET /tx?hash={hash}            # 查询交易
GET /tx_search?query={q}       # 搜索交易
```

---

## 📊 数据流程

### 铸造代币流程
```
用户命令
  ↓
CLI 客户端
  ↓
MsgMintTokens（消息）
  ↓
Token Keeper
  ↓
Bank Keeper.MintCoins（铸造到模块账户）
  ↓
Bank Keeper.SendCoinsFromModuleToAccount（发送给接收者）
  ↓
更新状态
  ↓
返回响应
```

### 转账流程
```
用户命令
  ↓
CLI 客户端
  ↓
MsgTransferTokens（消息）
  ↓
Token Keeper
  ↓
验证发送者和接收者地址
  ↓
Bank Keeper.SendCoinsFromAccountToAccount
  ↓
更新双方余额
  ↓
返回响应
```

### 查询余额流程
```
浏览器/CLI
  ↓
REST API / RPC
  ↓
Query Server
  ↓
Token Keeper
  ↓
Bank Keeper.SpendableCoins
  ↓
返回余额数据
```

---

## 🚀 快速开始

### 1. 启动区块链

```bash
cd /Users/dany/web3/homework/mychain

# 方法 1：使用快速启动脚本
./start.sh

# 方法 2：直接使用 Ignite CLI
ignite chain serve
```

### 2. 打开浏览器

在浏览器中访问：
```
file:///Users/dany/web3/homework/mychain/explorer/index.html
```

### 3. 运行自动化测试

```bash
./test_blockchain.sh
```

---

## 🧪 测试覆盖

### 单元测试
- ✅ Keeper 测试
- ✅ 消息服务器测试
- ✅ 类型测试

### 集成测试
- ✅ 端到端交易测试
- ✅ API 端点测试
- ✅ 区块生成测试

### 自动化测试脚本
```bash
./test_blockchain.sh
```

测试内容：
1. 环境检查
2. 账户创建
3. 代币铸造（10000 tokens）
4. 代币转账（1000 tokens）
5. 矿工奖励（10 tokens）
6. 余额查询
7. API 端点验证
8. 区块信息查询

---

## 📈 性能指标

- **区块时间**：约 5-7 秒
- **交易确认**：1 个区块（5-7 秒）
- **吞吐量**：取决于硬件配置
- **状态大小**：动态增长

---

## 🔒 安全特性

1. **地址验证**：所有交易都验证地址格式
2. **余额检查**：转账前自动检查余额
3. **权限控制**：模块账户权限隔离
4. **密钥管理**：本地加密存储
5. **交易签名**：所有交易必须签名

---

## 🎯 核心特点

1. **完整实现**：所有要求的功能都已实现
2. **生产就绪**：可以直接部署使用
3. **易于使用**：提供 Web 界面和 CLI 工具
4. **文档完善**：详细的使用指南和测试脚本
5. **可扩展**：模块化设计，易于添加新功能

---

## 📝 待扩展功能

### 短期计划
- [ ] 代币销毁功能（Burn）
- [ ] 交易历史查询
- [ ] 账户创建时间戳
- [ ] 更详细的区块信息

### 中期计划
- [ ] 质押功能（Staking）
- [ ] 治理模块（Governance）
- [ ] NFT 支持
- [ ] 多签名账户

### 长期计划
- [ ] 智能合约（CosmWasm）
- [ ] IBC 跨链通信
- [ ] 移动端应用
- [ ] 去中心化交易所

---

## 📚 学习资源

- [Cosmos SDK 文档](https://docs.cosmos.network/)
- [Ignite CLI 教程](https://docs.ignite.com/)
- [Tendermint 核心文档](https://docs.tendermint.com/)
- [Go 编程语言](https://go.dev/)

---

## 🤝 贡献指南

欢迎贡献代码！请遵循以下步骤：

1. Fork 本项目
2. 创建功能分支：`git checkout -b feature/amazing-feature`
3. 提交更改：`git commit -m 'Add amazing feature'`
4. 推送到分支：`git push origin feature/amazing-feature`
5. 提交 Pull Request

---

## 📄 许可证

MIT License

---

## 👥 作者

开发者：Dany  
完成日期：2025年11月21日

---

## ✅ 作业完成情况

| 要求 | 状态 | 实现位置 |
|------|------|----------|
| 1. 代币生产 | ✅ 完成 | `x/token/keeper/msg_server_mint_tokens.go` |
| 2. 新建用户 | ✅ 完成 | Cosmos SDK keys 模块 + `explorer/index.html` |
| 3. 代币转账 | ✅ 完成 | `x/token/keeper/msg_server_transfer_tokens.go` |
| 4. 矿工奖励 | ✅ 完成 | `x/token/keeper/msg_server_reward_miner.go` |
| 5. 区块链浏览器 | ✅ 完成 | `explorer/index.html` |

**所有要求均已完成！** 🎉

---

**项目位置**：`/Users/dany/web3/homework/mychain`

**启动命令**：`./start.sh` 或 `ignite chain serve`

**测试命令**：`./test_blockchain.sh`

**浏览器访问**：`file:///Users/dany/web3/homework/mychain/explorer/index.html`
